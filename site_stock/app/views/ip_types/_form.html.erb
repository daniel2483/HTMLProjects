<%= form_for(@ip_type) do |f| %>
  <% if @ip_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ip_type.errors.count, "error") %> prohibited this ip_type from being saved:</h2>

      <ul>
      <% @ip_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :DESCRIPCION %><br />
    <%= f.text_field :DESCRIPCION %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
