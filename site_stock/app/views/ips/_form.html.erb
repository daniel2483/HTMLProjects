<%= form_for(@ip) do |f| %>
  <% if @ip.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ip.errors.count, "error") %> prohibited this ip from being saved:</h2>

      <ul>
      <% @ip.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :IP %><br />
    <%= f.text_field :IP %>
  </div>
  <div class="field">
    <%= f.label :ID_SITES %><br />
    <%= f.number_field :ID_SITES %>
  </div>
  <div class="field">
    <%= f.label :ID_IP_TYPES %><br />
    <%= f.text_field :ID_IP_TYPES %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
